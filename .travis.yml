language: node_js
node_js:
  - 8
script: eleventy --pathprefix="/eleventy-base-blog/"
deploy:
  provider: netlify
  site: <site>
  auth: <encrypted auth>
  edge: true # opt in to dpl v2
  dir: "dist/"
