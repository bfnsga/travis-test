language: node_js
node_js:
 - "node"
script:
  - eleventy
  - postcss styles.css -o output.css
deploy:
  provider: netlify
  site: $NETLIFY_SITE
  auth: $NETLIFY_AUTH
  edge: true
  dir: "dist/"
  prod: true
